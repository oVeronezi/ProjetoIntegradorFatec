@model ControleDietaHospitalarUnimedJau.Models.Entrega

@{
    ViewData["Title"] = "Registrar Nova Entrega";
}

<h1>Registrar Nova Entrega</h1>

<h4>Entrega</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @* <div class="form-group">
                <label asp-for="HoraInicio" class="control-label"></label>
                <input asp-for="HoraInicio" class="form-control" />
                <span asp-validation-for="HoraInicio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HoraFim" class="control-label"></label>
                <input asp-for="HoraFim" class="form-control" />
                <span asp-validation-for="HoraFim" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StatusValidacao" class="control-label"></label>
                <input asp-for="StatusValidacao" class="form-control" />
                <span asp-validation-for="StatusValidacao" class="text-danger"></span>
            </div> *@
            <div class="form-group">
                <label asp-for="Observacao" class="control-label"></label>
                <input asp-for="Observacao" class="form-control" />
                <span asp-validation-for="Observacao" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="IdPaciente" class="control-label"></label>
                <select asp-for="IdPaciente" class="form-control" asp-items="ViewBag.IdPaciente">
                    <option value="">-- Selecione um Paciente --</option>
                </select>
                <span asp-validation-for="IdPaciente" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="IdCopeira" class="control-label"></label>
                <select asp-for="IdCopeira" class="form-control" asp-items="ViewBag.IdCopeira">
                    <option value="">-- Selecione uma Copeira --</option>
                </select>
                <span asp-validation-for="IdCopeira" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="IdBandeja" class="control-label"></label>
                <select asp-for="IdBandeja" class="form-control" asp-items="ViewBag.IdBandeja">
                    <option value="">-- Selecione uma Bandeja --</option>
                </select>
                <span asp-validation-for="IdBandeja" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Criar" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Voltar à Lista</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}